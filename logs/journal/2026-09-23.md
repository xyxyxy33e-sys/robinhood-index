# SPY 7DTE Journal — 2026-09-23
mode: dry_run | settled cash: n/a (sizing uses PAPER equity per config) | VIX: 14.29 (prior close 14.21, Δ +0.08 pts/day / +0.56%, accel +0.74 pts/day² — not notable)

## Phase 0 — Bootstrap (09:08 ET)
- `git fetch` + checkout `claude/robinhood-day-options-strategy-y8eskp`, pulled clean — up to date with origin (last commit `f139f47`).
- Confirmed weekday (Wednesday), market open today. `mode: dry_run` confirmed.
- `get_option_positions(nonzero=true, account=576391551)` → **empty**, as expected under `dry_run`.
- **CRITICAL FINDING: the entire `data/` directory was gone** — `strategy_calc.py paper` failed with `FileNotFoundError: data/paper_ledger.json`, and `ls data/` reported "No such file or directory". This is the local, gitignored working state (paper ledger + daily bars/context files) — it does not persist across a container reclaim, and everything points to that having happened: three sessions in a row now (9/18, 9/21, 9/22) had a mid-session `ScheduleWakeup` silently fail to deliver for 6-7+ hours, while the two account-level cron `Routine` triggers (`0dte-morning-session`, `0dte-failsafe-closeout`) fired exactly on schedule every single time. The working theory: this session's container gets reclaimed during those multi-hour idle gaps, a self-scheduled wake targeting the old container is lost, and only the cron triggers — which appear able to start a fresh container/session — get through. Today's total data loss is the clearest evidence yet for that theory, though it can't be confirmed from inside this session.
- **Recovery**: `data/paper_ledger.json` is the one file in `data/` that cannot be safely re-bootstrapped from scratch (unlike the daily bars files, which are just re-fetched market data) — it is the compounding record of every realized paper trade since 2026-08-17. Rather than lose that history, delegated a subagent to extract every realized trade's exact fields (fill, exit, qty, pnl_usd, return_pct, reason, dates, instrument_id where recorded) from the git-committed `logs/journal/*.md` files (2026-08-17 through 2026-09-22) — the journals are the authoritative, git-persisted record per this project's own convention ("journal it") and were never lost. The subagent found exactly 17 realized trades summing to **-$247.00**, matching the last known `paper` output before the loss (2026-09-21 close: realized_trades 17, realized_pnl_usd -247.00, paper_equity_usd 11611.54). I independently re-derived `pnl_usd` and `return_pct` from each trade's own recorded `fill`/`exit`/`qty` for all 17 rows before trusting the reconstruction — every one matched exactly, and the running sum matches at -$247.00. Wrote the reconstructed ledger to `data/paper_ledger.json` (starting_balance_usd 11858.54, ledger_start_date 2026-08-17, open_positions: [], 17 realized rows) and confirmed `strategy_calc.py paper` now reproduces the exact known state: realized_trades 17, realized_pnl_usd -247.0, paper_equity_usd **11611.54**, 0 open positions. Seven of the earliest trades (opened 8/27-9/2, before the `ledger open/close` CLI tooling existed 2026-09-02) have `instrument_id: null` — that field was genuinely never recorded for those and is not fabricated.
- `strategy_calc.py paper` post-recovery: 0 open positions — flat entering today, consistent with 2026-09-21's close.
- Re-bootstrapped `data/2026-09-23-bars.json` fresh (prior closes: SPY 773.38, QQQ 747.46, IWM 287.21) — this file is disposable/re-fetchable, unlike the ledger.

## Regime
gap SPY: -0.12% (premarket $772.51 vs prior close $773.38) · news veto: none (top 10 `get_equity_news` headlines — no FOMC/CPI/NFP; escalating unscheduled Iran-tension headline this morning, "Trump Threatens to 'Annihilate' Iran at UNGA", futures dipped on it — flagged as elevated tail-risk context per the 9/18 precedent, not a mechanical veto) · VIX 14.29 < 35 · gap well under `gap_limit_pct` 1.5% · tradeable: yes

## Volatility baseline
VIX: 14.29 · rv5: 13.69% · rv10: 12.37% · rv20: 10.53% (21 daily SPY closes via `strategy_calc.py rvol`) — IV/RV10 ≈ 1.15x, unremarkable.

## External context (diagnostic)
Not gathered this wake (prioritized the data-loss recovery above) — will pick up Webull/Alpha Vantage context calls if time allows before 09:45 ET, otherwise skip for today; diagnostic only, gates nothing.

## Signal history
| time | SPY | QQQ | IWM | market | note |
|---|---|---|---|---|---|
| 09:13 ET | -14.6 (gap -5.6 + premarket -1.7) | 0.0 | 0.0 | -4.9 | Pre-open only (gap+premarket, rescaled) — **not actionable before 09:30 ET**. Small gap down, well below threshold. |

## Wake delivery log
| wake | scheduled | delivered | lag | cadence |
|---|---|---|---|---|

## Volatility snapshot per signal (diagnostic)
| time | contract | taken? | IV | rv10 | IV/RV | delta | theta | vega | outcome |
|---|---|---|---|---|---|---|---|---|---|

## Entry & blocked-signal extremity tracking (diagnostic)
| time | contract/direction | status | score | drive | drive_pct | clamped? | range_pos | extremity |
|---|---|---|---|---|---|---|---|---|---|

## Persistence gate log (REAL gate)
| time | score | trailing run (min) | gate met? | action |
|---|---|---|---|---|

## Re-entry distance tracking (diagnostic — every same-day re-entry)
| time | symbol/direction | first entry price | reentry price | extended_pct | minutes since first entry | outcome |
|---|---|---|---|---|---|---|

## Shadow 0DTE leg (diagnostic — never traded)
| time | 7DTE contract | 0DTE contract | 7DTE mid | 0DTE mid | 0DTE qty @ budget | stop/TP hit? | terminal (13:30) |
|---|---|---|---|---|---|---|---|

## Signal-decay tracking (diagnostic — every wake while open)
| time | contract | entry score | score now | retained % | triggered | option mid | hypothetical P&L if exited here |
|---|---|---|---|---|---|---|---|

## Phase 4 — monitoring
| time | note |
|---|---|

## Trades
| # | contract | qty | fill | stop id | exit | exit px | P&L | reason |
|---|---|---|---|---|---|---|---|---|

## Phase 5 — EOD verification (HH:MM ET)
(pending)

## End of day
flat by: · realized P&L: $X · trades: N/6 · deviations:

## Deviations from routine, logged for the owner
**Paper ledger data loss and reconstruction, 2026-09-23.** This session's entire local `data/` directory (gitignored, not git-persisted) was gone at bootstrap — most likely a container reclaim during one of the last three multi-hour intraday wake gaps (9/18, 9/21, 9/22). The daily bars/context files are trivially re-fetchable and were simply recreated, but `data/paper_ledger.json` held the only record of the compounding paper-equity history since 2026-08-17 and is not re-derivable from any live market data. Reconstructed it from the git-committed journal files (the authoritative record per this project's convention) via careful extraction + independent verification of every trade's arithmetic; the result reproduces the last known state exactly (17 realized trades, -$247.00, $11,611.54 equity) with no fabricated numbers. **Flagging for the owner**: this is now strong, concrete evidence for the theory behind the three prior missed-wake incidents — a reclaimed container would explain both symptoms (self-scheduled wakes silently failing, and now local state being wiped) in one mechanism, and it means every prior "no real risk, dry_run only" reassurance about missed wakes has been correct only by luck of `mode: dry_run` — a live-mode position would have had no local record of its own stop parameters to recover either, though its broker-side GTC stop order would still exist independently of this container. Recommend checking whether this session's container has an idle-reclaim timeout shorter than the multi-hour gaps between scheduled trading-day events, and whether `data/` (or at least `paper_ledger.json`) should be moved somewhere persistent rather than gitignored.
